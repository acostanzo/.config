# CLAUDE.md

@AGENTS.md

## Claude Code

Claude Code reads `CLAUDE.md` at session startup. `AGENTS.md` is the shared
source of truth for this repo's operating guide, so keep repo-wide instructions
there and keep this file limited to Claude-specific behavior.

- Preserve the `@AGENTS.md` import at the top of this file.
- If a repository convention changes, update `AGENTS.md` first.
- Add notes here only when they are specific to Claude Code loading, memory, or
  tool behavior.
- When editing files that contain powerline glyphs, verify the glyphs are
  preserved in the diff before committing.
